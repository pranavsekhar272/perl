#!/usr/local/bin/perl

# Write a Perl program that uses the while statement to print out the first 10 numbers (1-10) in ascending order.


$number = 1;


while ($number <= 10) {
	print ($number, "\n");
	$number = $number + 1;
}
